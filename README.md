# DP 背包 OJ

基于《背包问题九讲》（崔添翼，CC BY-NC-SA）的个人自学用在线评测与练习平台。
技术方案详见 `docs/DP-OJ-技术方案与功能大纲.md`，整体验收详见 `docs/验收清单.md`。

## 快速开始

**一键启动（推荐）**：双击 `start.bat`（首次需先执行一次 `pnpm install`）。

- 自动检查 Node.js/pnpm/Go 环境 → 构建后端 → 分别打开后端(3000)与前端(5173)窗口 → 自动打开浏览器 http://localhost:5173
- 停止：双击 `stop.bat`（结束 3000/5173 端口上的前后端进程）

**手动启动**（开发模式）：

```bash
pnpm install
pnpm dev        # server: http://localhost:3000/api/system/health ｜ web: http://localhost:5173
```

打开 http://localhost:5173 即可开始刷题。

## 安全说明（单机自用）

- 后端**只监听 127.0.0.1**、CORS 仅放行本地前端来源，不向局域网开放。
- 判题默认走**本地进程沙箱**：超时强杀进程树 + 输出截断；**内存限制为尽力而为**（Windows 无轻量 Job Object 方案），需要严格内存/断网隔离请启用 Docker 沙箱。
- 提交代码本质上是"在你的机器上以你的权限运行程序"——这是 OJ 的正常功能，请只运行自己写的代码。

## 数据存放位置

- **题库**：`data/problems/`（题面 statement.md + 配置 config.json + 测试数据 cases/，随代码仓库管理）
- **数据库**：`data/oj.db`（SQLite，存题目元数据、测试用例索引、提交记录与判题结果；WAL 模式，另伴生 oj.db-wal / oj.db-shm 两个文件）
- **提交的代码**：`data/submissions/<提交ID>/`
- 服务器从任意目录启动都会定位到同一份数据（`DATA_DIR`/文件位置多级回退解析），也可用环境变量 `DATA_DIR` 强制指定。

## 功能

- **题库**：34 题覆盖九讲（每讲至少 3 题：A1-A12 基础 + B1-B8 扩展 + C1-C14 分类扩充），题面含 LaTeX 公式
- **九讲分类筛选**：题目列表页按「全部 / 第 1-9 讲」一键过滤
- **在线编写**：Monaco 编辑器，支持 C++14 / Python 3 / Go / Java 语法高亮与草稿自动保存
- **即时测试**：「运行样例」一键跑题面样例；「自定义测试」任意 stdin 快速运行（四语言 <2s）
- **提交判题**：编译 → 逐组沙箱运行 → 输出比对（行末空白不敏感），首败组即终态
- **结果反馈**：判题结果码 + 总耗时 + 逐组明细 + CE 编译器全文 + WA 期望/实际对比 + RE stderr
- **提交记录**：最近 50 条提交，点击查看逐组判题详情
- **做题计时**：每题独立计时器（开始/暂停/继续/停止，跨刷新恢复）+ 自动计时开关 + 一键重置本题
- **主题**：四套主题（深蓝暗色/明亮/纯黑/护眼纸色）+ 自动跟随系统亮暗
- **复习计划与错题本**：AC 自动进入间隔重复队列（首次 1 天后复习，SM-2 按记忆效果拉长间隔），非 AC 自动进错题本；复习页一键「开始复习」（清草稿+计时归零重做），AC 后自评调度下次复习（/review 页面）
- **分层提示**：每题三层提示（思路方向 → 状态与转移 → 关键代码）逐层展开、独立记录；AC 后锁定防自我欺骗
- **知识卡片**：九讲 28 张核心知识点卡片（正面提问背面答案），翻转 + 记忆自评，与题目共用间隔重复队列（/cards 页面）
- **进度统计**：九讲掌握度仪表盘（每题 AC/总数/进度条/平均首过用时）+ 提交热力图 + 结果码分布 + 累计专注时长（/dashboard 页面）
- **DP 填表可视化**：01/完全/多重三种模型的 dp 数组填表演示——逐步/自动回放，高亮当前格与转移来源（01 看上一行、完全看本行），一维/二维双视角，可改容量与物品数据实时重演（/visualizer 页面）
- **系统状态**：判题机工具链自检（/system 页面）

## 判题结果码

| 码 | 含义 |
|---|---|
| AC | 通过 |
| WA | 答案错误（输出超限 OLE 亦归 WA） |
| TLE | 超出时间限制（单组限时 x 语言系数） |
| MLE | 超出内存限制 |
| RE | 运行时错误（非零退出） |
| CE | 编译错误（附编译器全文） |
| SE | 系统错误 |

## 沙箱模式（Docker 是否需要？）

- **本地进程（默认）**：spawn 子进程 + 超时强杀进程树 + 输出截断，零外部依赖；Windows 下内存限制为尽力而为。
- **Docker 容器（可选）**：精确内存限制（--memory）、断网（--network=none）、限核限进程数。

**平时刷题完全不需要打开 Docker**——默认就走本地进程沙箱，`start.bat` 也不依赖 Docker。只有当你想启用更严格的容器隔离时才需要 Docker Desktop：

```powershell
.\data\docker\build-judge-image.ps1   # 构建判题镜像 dp-oj/judge-env（首次需 Docker Desktop）
$env:JUDGE_RUNNER = 'docker'            # 切换到容器沙箱（每次启动前保持 Docker Desktop 运行）
pnpm dev:server
```

## 题库（34 题，九讲分类可筛选）

- **A1-A12 基础题**（对应九讲）：采药 / 疯狂的采药 / 宝物筛选 / 樱花 / 勤工俭学 / 通天之分组背包 / 金明的预算方案 / 选课 / 装箱问题 / 货币兑换 / 骨牌收集家 / 打包清单
- **B1-B8 扩展题**：矿石巨量（单调队列）/ 有线电视网（树形背包）/ 社团活动（二维+分组）/ 赏花计数（方案数）/ 精打细算（恰好装满+字典序）/ 同余凑数（背包+数论）/ 装备树（依赖森林）/ 泛化物品求和
- **C1-C14 分类扩充题**（九讲每类至少 3 题）：满载而归 / 预算最小化 / 装载方案数 / 零钱最少张 / 满载采药 / 装得下吗（bitset 可行性）/ 恰好赏花 / 满负荷工作 / 恰好分组 / 自由搭配（附件不限依赖）/ 求最优的次优（完全第 K 优）/ 矿石的摆法（多重方案数）/ 价值达标（混合价值维度）/ 分组的摆法（分组方案数）

题目列表页提供**九讲分类筛选**：选择某一讲后仅显示该讲题目。

## 目录

- `apps/web`  React + Vite 前端（Monaco 编辑器）
- `apps/server` NestJS 后端（题库/提交/判题编排/判题队列）
- `packages/judge` 判题引擎（编译/沙箱/比对/首败终态/CLI/DockerRunner）
- `packages/common` 判题结果码、语言配置、共享类型
- `data/problems` 题库（题面 + 配置 + 测试数据）
- `data/seeds` 测试数据生成器（固定种子可复现）与 C++ 参考解
- `data/docker` 判题镜像 Dockerfile 与构建脚本

## 常用命令

```bash
pnpm dev                                   # 前端 + 后端
pnpm --filter @dp-oj/judge test            # 判题引擎单元测试（15 项）
node packages/judge/dist/cli.js --lang cpp --source x.cpp --cases dir   # 判题 CLI
node data/seeds/generators/gen-b1-b8.mjs   # 重新生成测试数据（固定种子，另见 gen-c1-c5 / gen-c6-c10 / gen-c11-c14）
```

## 里程碑进度

- [x] M0 环境与脚手架（✅ 2026-08-16）
- [x] M1 题库管理：A1/A2 上线（✅ 2026-08-16）
- [x] M2 判题引擎核心：15 项单测 + A1 四结果码端到端（✅ 2026-08-16）
- [x] M3 多语言 + 结果反馈：四语言 HTTP 全链路 AC（✅ 2026-08-16）
- [x] M4 Monaco 编辑器 + 即时测试（✅ 2026-08-16）
- [x] M5 题库全量：20 题对拍 20/20 AC（✅ 2026-08-16）
- [x] M6 打磨收尾：提交记录页 / Docker 沙箱开关 / 文档（✅ 2026-08-16）
- [x] 主题系统：自动昼夜 + 四套主题切换（✅ 2026-08-17）
- [x] 计时与重置：每题独立计时器 + 一键恢复初始代码（✅ 2026-08-17）
- [x] 题库扩充 + 分类筛选：34 题对拍 34/34 AC，九讲分类页（✅ 2026-08-17）
- [x] 一键启动 start.bat / stop.bat（✅ 2026-08-17）
- [x] 全面审计修复：数据目录 cwd 无关化 / 仅监听 127.0.0.1 / 判题 close 定稿与完整输出比对（消除大输出误判）/ 队列状态机与重启恢复 / 临时目录清理 / 前端计时器与面板跨题污染 / Docker 模式 Go 修复 / health 工具链探测修正（✅ 2026-08-17）
- [x] 学习闭环：复习计划与错题本（SM-2 间隔重复 + 自评调度）+ 34 题三层提示系统（AC 锁定）（✅ 2026-08-17）
- [x] 知识卡片（九讲 28 张，翻转 + 自评 + SM-2）+ 进度仪表盘（九讲掌握度/热力图/结果分布/专注时长）（✅ 2026-08-17）
- [x] DP 填表可视化：01/完全/多重 双视角逐步回放 + 可编辑输入（✅ 2026-08-17）

## 协议说明

原文档《背包问题九讲》为 CC BY-NC-SA 协议；本题库题面为原创改写、测试数据自行生成，仅用于个人学习。
