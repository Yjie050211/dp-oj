/**
 * 知识卡片：九讲每讲 3 张（正面问题 / 背面答案），喂入同一套 SM-2 复习队列
 */

export interface KnowledgeCard {
  id: string;
  lectureNo: number;
  lectureTitle: string;
  front: string;
  back: string;
}

const C = (id: string, lectureNo: number, lectureTitle: string, front: string, back: string): KnowledgeCard => ({ id, lectureNo, lectureTitle, front, back });

export const CARDS: KnowledgeCard[] = [
  C("l1-1", 1, "01 背包问题", "01 背包用一维数组优化时，容量为什么要倒序遍历？", "因为每件物品只有一件。倒序保证 dp[j-w] 还是「未考虑当前物品」的旧值；若正序，dp[j-w] 已包含当前物品，一件物品会被重复取多次，退化成完全背包。"),
  C("l1-2", 1, "01 背包问题", "01 背包要求「恰好装满」时如何初始化？", "dp[0]=0，其余全部设为 -inf（求最大）或 INF（求最小），转移时跳过不可达状态；最后 dp[V] 仍为 -inf 表示无法恰好装满。"),
  C("l1-3", 1, "01 背包问题", "01 背包的一维转移方程是什么？", "dp[j] = max(dp[j], dp[j-w] + v)，j 从 V 到 w 倒序，初始全 0（不要求恰好装满）；答案 dp[V]。"),
  C("l2-1", 2, "完全背包问题", "完全背包为什么把容量改成正序遍历就对了？", "正序时 dp[j-w] 已经包含「取过当前物品」的状态，允许同一物品被连续取多次；每件物品无限，正好符合语义。"),
  C("l2-2", 2, "完全背包问题", "完全背包求方案总数怎么做？", "dp[0]=1，其余 0；对每种物品正序：dp[j] += dp[j-w]，注意取模防溢出。"),
  C("l2-3", 2, "完全背包问题", "完全背包要求「恰好装满」时如何初始化？", "dp[0]=0，其余 -inf；正序转移；dp[V] 为 -inf 表示无法恰好装满。"),
  C("l3-1", 3, "多重背包问题", "多重背包的二进制拆分是什么原理？", "m 件拆成 1、2、4、...、余数 共 O(log m) 块，每块代表 2^k 件；这些块能组合出 0..m 的任意数量，于是转成 01 背包逐块倒序。"),
  C("l3-2", 3, "多重背包问题", "多重背包 O(NV) 的单调队列优化怎么做？", "对容量按 c 取模分组，组内 dp[j] = max(dp[j-kc] + k*w)，k<=m；用单调队列维护 dp[j-kc] 的最大值（入队加偏移修正），队首超出 m 个弹出。"),
  C("l3-3", 3, "多重背包问题", "二进制拆分能用于「方案计数」吗？", "不能。拆分后不同的块组合可能对应同一种数量取法，计数会错；方案计数要用滑动窗口前缀和 h[v] = g[v] + h[v-c] - g[v-(m+1)c]。"),
  C("l4-1", 4, "混合三种背包问题", "混合背包（01+完全+多重）怎么处理？", "按类型分派：01 倒序、完全正序、多重二进制拆分后倒序；三种过程共用同一个 dp 数组，顺序保证每类各自正确。"),
  C("l4-2", 4, "混合三种背包问题", "什么是「价值维度」的背包？", "把价值当容量、花费当价值：dp[s] = 总价值达到 s 的最小花费；价值超过上限时截断到上限（min(S, s+w)），求达到目标价值的最小费用。"),
  C("l4-3", 4, "混合三种背包问题", "混合背包 + 恰好装满怎么初始化？", "dp[0]=0 其余 -inf（或 INF），按类型分派三类过程，最后检查 dp[V] 是否可达。"),
  C("l5-1", 5, "二维费用的背包问题", "二维费用背包的转移方程？", "dp[j][k] = max(dp[j][k], dp[j-a][k-b] + v)；两维容量都要倒序（01 语义），初始 dp[0][0]=0。"),
  C("l5-2", 5, "二维费用的背包问题", "二维费用 + 恰好装满怎么初始化？", "只有 dp[0][0]=0，其余全部 -inf；两维倒序转移，最后看 dp[V][U]。"),
  C("l5-3", 5, "二维费用的背包问题", "「隐含」的第二维费用有哪些例子？", "物品个数（限取 K 件）、时间+体力、金钱+积分等；任何两种同时消耗的资源都可以当作两维费用。"),
  C("l6-1", 6, "分组的背包问题", "分组背包的三重循环顺序是什么？", "外层：组；中层：容量 j 从 V 到 0 倒序；内层：组内每件物品更新 dp[j]。顺序不能乱。"),
  C("l6-2", 6, "分组的背包问题", "分组背包为什么容量要倒序？", "保证每组至多选一件：倒序让组内转移互不影响；若正序，组内前一件的结果会被后一件继续利用，变成一组可选多件。"),
  C("l6-3", 6, "分组的背包问题", "分组背包求方案数怎么做？", "每组先拷贝 dp 作为转移来源（cur），再 dp[v+c] += cur[v]（加法取模）；拷贝数组保证每组至多一件。"),
  C("l7-1", 7, "有依赖的背包问题", "附件数量少（如 <=2）的依赖背包怎么做？", "把「只买主件 / 主件+附件1 / 主件+附件2 / 主件+全部附件」枚举成选项集，对每个主件的选项集做分组背包。"),
  C("l7-2", 7, "有依赖的背包问题", "附件数量不限的依赖背包怎么做？", "对每个主件的附件集合先做一次 01 背包（容量 N-v主），得到「选主件+任意附件」的全部可选组合（物品组），再做分组背包。"),
  C("l7-3", 7, "有依赖的背包问题", "树形依赖（如选课）怎么做？", "后序遍历，dp[u][j] 为 u 子树选 j 个的最大价值；合并儿子 v 时容量倒序（分组背包树形版）：dp[u][j]=max(dp[u][j], dp[u][j-k]+dp[v][k])。"),
  C("l8-1", 8, "泛化物品", "什么是泛化物品？", "一个物品被抽象成「容量 -> 价值」的函数 f(w)（给多少容量返回多少价值）；01 物品、完全物品、背包里的物品组合都是泛化物品。"),
  C("l8-2", 8, "泛化物品", "泛化物品的和怎么求？", "h[k] = max(f[i] + g[j])，i+j=k；即逐容量的 max-plus 卷积，复杂度 O(V^2)。"),
  C("l8-3", 8, "泛化物品", "树形背包与泛化物品有什么关系？", "每个子树就是一个泛化物品（子树占用容量换价值），合并子树 = 求泛化物品的和，这正是树形背包的数学本质。"),
  C("l9-1", 9, "背包问题问法的变化", "「恰好装满」的所有变式靠什么统一？", "初始化：dp[0]=0，其余 -inf（最大值问题）或 INF（最小值问题）；不可达状态一律跳过，最后检查 dp[V]。"),
  C("l9-2", 9, "背包问题问法的变化", "求第 K 优解怎么做？", "dp 数组每项是一个有序的前 K 大序列；转移时把 dp[j-w]（整体加 v）与 dp[j] 双指针归并取前 K；完全背包正序、01 倒序。"),
  C("l9-3", 9, "背包问题问法的变化", "输出字典序最小的最优方案怎么做？", "把物品按编号逆序填表（dp[i][j] = 考虑 i..N 物品），再从 1 开始贪心回溯：若 dp[i][j] == dp[i+1][j-w]+v 则必选 i。"),
  C("l9-4", 9, "背包问题问法的变化", "最优方案总数怎么统计？", "维护 (最大价值, 方案数) 二元组：转移更大则替换并继承方案数，相等则方案数相加；初始化 dp[0]=(0,1)。"),
];
