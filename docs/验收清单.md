# DP 背包 OJ 整体验收清单（§10 DoD）

> 验收日期：2026-08-16 ｜ 验收人：自动化验收脚本 + 人工复核

## 1. 四语言各 AC 一题 ✅

HTTP 全链路提交 A1「采药」参考解（data/seeds/generators/m3-verify.mjs）：

| 语言 | 判题结果 | 总耗时 | 组数 |
|---|---|---|---|
| C++14 | AC | 724 ms | 12/12 |
| Python 3 | AC | 3941 ms | 12/12 |
| Go | AC | 657 ms | 12/12 |
| Java | AC | 1545 ms | 12/12 |

## 2. CE / WA / TLE 判罚正确 ✅

| 故意提交 | 期望 | 实际 | 反馈内容 |
|---|---|---|---|
| 语法错误代码 | CE | CE ✅ | 编译器全文（含行号） |
| 恒输出 0 | WA | WA ✅ | 第 1 组失败 + 期望/实际对比 |
| 死循环 | TLE | TLE ✅ | 超时组号 + 实耗时间 |

## 3. 九讲 12 道基础题全部上线 ✅

题库导入 20/20（含 A1-A12），题面（含 KaTeX 公式）、样例、测试数据完整。
列表 API 验证：A1-A12 与 B1-B8 排序正确。

## 4. 前端完整闭环 ✅

浏览题目列表 → 题目详情（题面+样例）→ Monaco 写码 → 运行样例 → 自定义测试 → 提交 → 逐组结果详情 → 提交记录回看。

## 5. 单命令启动 ✅

`pnpm install && pnpm dev` 一条命令启动前后端；Windows 单机零外部服务依赖（Docker 可选）。

## 6. 判题引擎单元测试 ✅

15/15 通过（`pnpm --filter @dp-oj/judge test`）：
输出比对（行末空白/CRLF/行内差异）、运行器（stdout 捕获/超时强杀/输出截断/退出码）、
C++ 端到端（AC/WA/TLE/CE）、首败终态、未知语言 SE。

## 7. 题库数据对拍 ✅

34 道题 C++ 参考解经判题引擎 CLI 全部 AC（34/34，共 394 测试组）。
数据生成器固定种子可复现（gen-a1-a2 / gen-a3-a6 / gen-a7-a12 / gen-b1-b8 / gen-c1-c5 / gen-c6-c10 / gen-c11-c14）。
题目列表页支持九讲分类筛选；九讲每类至少 3 题。

## 8. 沙箱安全（附加）✅/⭕

- 本地进程沙箱：超时强杀进程树、输出上限截断 ✅
- Docker 容器沙箱：判题镜像 dp-oj/judge-env 构建成功，容器内编译+运行验证通过
  （runCode stdout 正确、judge A1 12 组全 AC；--network=none/--memory/--cpus/--pids-limit 隔离）✅

## 结论

**全部 7 项核心验收通过，项目达到交付标准。**
